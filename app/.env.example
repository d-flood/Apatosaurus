# Example env file for local-first MVP runtime
# No CouchDB configuration is required.

# Dropbox OAuth PKCE. The redirect URI must exactly match your Dropbox app settings.
PUBLIC_DROPBOX_CLIENT_ID=
PUBLIC_DROPBOX_REDIRECT_URI=http://localhost:5173/projects
