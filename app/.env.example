# Example env file for local-first MVP runtime
# No CouchDB configuration is required.

# Collation route feature flag.
# Local development default is enabled if unset; production/preview default is disabled.
PUBLIC_ENABLE_COLLATION=true
