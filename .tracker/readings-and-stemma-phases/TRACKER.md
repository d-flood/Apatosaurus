# Tracker for readings-and-stemma-phases

## Purpose

This document tracks the status of all tickets in the epic. Goal: make the CBGM data entry phases — Readings and local stemmata — completable, trustworthy, and worth completing. Today editorial decisions are recomputed from normalized-text equality on every write so they do not persist; the subreading attachment control silently no-ops for every case a scholar would reach for; the local stemma diagram overlays citation structure with genealogy in a fixed two-row layout and cannot be operated without a mouse; a lacuna is treated as a reading; nothing in either phase is undoable; and nothing anywhere consumes the result. Spec: `SPEC.md` in this directory.

The shape of the fix: separate the reading proposal from editorial decisions, reframe Readings around bulk witness selection, present the local stemma as an auto-laid-out single-source tree over arc-based storage, and end the flow in a Review phase with a reconstructive TEI apparatus validated against TEI P5. Geometry, the decision overlay, and apparatus rendering move into pure modules tested in the node project; the components become thin.

## Current Status

Overall status: `In Progress`

Current ticket: None

Last updated: 2026-08-15

## Ledger

Ticket 01 is a pure prefactor with no behaviour change — it makes everything after it a small change. Ticket 02 is the tracer bullet: it carries one decision kind end to end, proves the proposal/decision architecture, and fixes the headline bug on its own. Tickets 03, 04, and 05 then add decision kinds along the path 02 establishes and are **independent of each other** — they can run in parallel. Ticket 06 needs the completed reading model to render, and 07 consumes it. Ticket 08 deliberately delivers the accessible source-decision route before 09 adds pointer and keyboard graph gestures, so the stemma phase is fully operable even if 09 slips. Ticket 11 is the payoff and needs both the renderer and finished stemmata. Ticket 12 is research, gates nothing, and can run at any point.

The critical path is 01 → 02 → 05 → 08 → 10 → 11.

| Number | Filename | Status | Depends On |
| --- | --- | --- | --- |
| 01 | `01-extract-reading-proposal-module.md` | Completed | None |
| 02 | `02-durable-subreading-attachment-and-undo.md` | Completed | 01 |
| 03 | `03-non-attestation-out-of-reading-model.md` | Completed | 02 |
| 04 | `04-reading-types-and-certainty.md` | Completed | 02 |
| 05 | `05-lemma-establishment.md` | Completed | 02 |
| 06 | `06-segment-and-apparatus-renderer.md` | Completed | 03, 04, 05 |
| 07 | `07-readings-phase-cards-and-bulk-selection.md` | Completed | 06 |
| 08 | `08-local-stemma-source-decisions-and-layout.md` | Completed | 03, 05 |
| 09 | `09-stemma-graph-interaction.md` | Completed | 08 |
| 10 | `10-lemma-root-coupling-and-connectivity.md` | Completed | 08 |
| 11 | `11-review-phase-and-tei-apparatus-export.md` | Completed | 06, 10 |
| 12 | `12-research-connectivity-absolute-value.md` | Not Started | None |

## Notes

| Date | Note |
| --- | --- |
| 2026-08-15 | Ticket `11` completed. Review is the fifth, alignment-reachable phase, with the shared apparatus, local stemmata, divergence report, linked worklist, TEI export, and version commit. The new reconstructive apparatus exporter validates against TEI P5 all-modules and refuses incomplete editorial data. Primary, working, and checkpoint collation formats now fail loudly on stale builds; derived collation TEI is regenerated from canonical data and never trusted from project imports. |
| 2026-08-15 | Ticket `10` completed. Connectivity is a sparse, positive integer decision that defaults to `10`, with presets and free entry on the stemma phase. Lemma-posterior arcs are reported as a visible worklist violation without preventing source edits or mutating arcs when a lemma changes; the explicit reroot repair removes only those arcs in one undoable operation. |
| 2026-08-15 | Ticket `09` completed. The local-stemma diagram is a single roving tabstop of HTML buttons over a non-interactive SVG arc surface. Keyboard lift/move/place, cancellation, unclear/root/detach actions, drag reparenting, and canvas detachment all route through `setReadingSource`, announce their state changes, and preserve the ticket 08 list editor. The epic Playwright spec verifies the keyboard and pointer routes. |
| 2026-08-15 | Ticket `07` completed. The readings phase is no longer a table of rows but **cards anchored by their letter**, with subreadings nested inside their main reading's card and reading text edited in place (`input-ghost`, committed on blur, so ticket 02's undo coalescing is untouched and no per-keystroke commit was introduced). **Witness sigla are one selection group spanning every card**: the chips are flattened into a single sequence in reading order, so a range can start in reading `a` and end in reading `c`. That group is **one tabstop** — a roving `tabindex` with exactly one `0` — with arrow keys and `Home`/`End` moving inside it, `Space` toggling, `Shift`+arrow and `Shift`-click extending from the anchor, and `Escape` clearing. Selection lives in the component, never the store, which is what makes ticket 02's fence hold by construction: no selection change can reach `pushCommand`. **The verbs act on the whole selection once per gesture**: `moveWitnessesToReading(unitIndex, witnessIds, targetReadingId)`, `splitWitnessesIntoNewReading(unitIndex, witnessIds, { subreadingOf })`, and `mergeReadings(unitIndex, sourceReadingIds, targetReadingId)`, each **one undo entry**. Undo for proposal edits did not exist before: `setReadingsForUnit` wrote and marked unsaved but pushed nothing, so the new `commitReadingsForUnit` snapshots the `classifiedReadings` map before and after — a value copy, correct by construction, the same bargain the decisions overlay makes. Nothing was welded into the proposal: the bulk verbs edit witness membership only and still route through `setReadingsForUnit`, which goes on restoring every decision-overlaid field. **Ticket 08's stopgap is gone**: the per-witness reassign select — and with it the singular `moveWitnessToReading` and `splitWitnessFromReading`, whose only call sites were that select — has been deleted in favour of the real bulk verbs; the three specs that used the singular forms now call the bulk ones with a one-element array, so there is one way to do this and not two. **Non-attestation is not selectable**: it holds no reading id, so `attestingSelection` filters it out of every verb (a refusal, `no-attesting-witnesses`, not a silent no-op) and the component renders those sigla as plain text in a pinned panel, exactly as SPEC line 197 requires. Refusals all explain themselves in the live region, including the reorder reasons ticket 05 made `moveReadingByOffset` return (`at-boundary`, `different-group`), the split that would take every witness of the only reading involved (`whole-reading` — an empty husk beside an identical new reading records no distinction), and merging a reading into its own subreading (`target-under-source`, which would leave the survivor attached to a reading that no longer exists). **Rare operations moved into a per-card `<details>` menu** — reorder, elevate to lemma, add subreading, attach-as-subreading-of, reading type, certainty, delete (still blocked while witnesses attest, with the count and the reason stated) — and are absent from the card's primary surface, which now carries only the letter, the text, and the chips. Attachment stayed in the menu deliberately rather than being dropped: it is ticket 02's headline feature and losing its only route is the exact defect ticket 08 had to remediate. One shared-code fix, made at the source rather than worked around: `getReadingFamiliesForUnit` still assumed one level of subreading (recorded during ticket 06), so a chained subreading fell out of its family and out of the regularized-mode witness roll-up; it now walks the whole chain, matching what `relabelReadings` already did for labelling. `DotsThreeVertical` is registered in `optimizeDeps.include` per the repo rule. **Every new spec was proved by mutation against the pre-fix code, with all fixtures driven through `collateToAlignmentSnapshot()`/the real adapter — never hand-built cells.** Server (10 new): writing without an undo entry reddens the move and split undo specs; unfiltering `attestingSelection` reddens the non-attestation refusal; dropping the `whole-reading` guard, the `target-under-source` guard, the subreading reattachment, the merge union, and the `subreadingOf` option each redden exactly one spec; reverting families to direct children reddens the chained-subreading spec. Client (8 new, real chromium): giving every chip `tabindex=0` reddens the single-tabstop spec, removing arrow-key focus movement reddens it too; deleting the `Escape` branch, the `Shift` extension, the selection-wide move, the selection-wide split, and the selection-wide merge each redden their own spec; rendering non-attestation witnesses as chips reddens the non-attestation spec; silencing the reorder refusal reddens the per-card-menu spec; and making a chip toggle call `pushCommand` reddens the "one undo step" spec, which is the direct assertion that selection is not undoable. Server suite 680 tests (10 new), client browser project 257 tests (8 new), and `pnpm check` (0 errors) pass; the single client failure is the known unrelated `transcriptionEditorWorkarounds` flake, which passes in isolation and left a stray `__screenshots__/` directory, removed. `pnpm lint` is unchanged from baseline (the `no-useless-assignment` error in `reference-editions/insertion.ts` and 21 unused-var warnings). **Deviation, recorded and left unchecked rather than claimed**: the ticket's `e2e/collation-editorial-phases.spec.ts` was not written — no existing e2e reaches the readings phase, and building a transcription-to-alignment flow to get there is a ticket of its own — so the keyboard contract is asserted in the client project instead, in a real browser with real focus and real key events. Ticket 11 still owns the epic's Playwright spec and should fold these assertions in. **Not verified by hand in the running app**: `pnpm run dev` was not driven; the contract is covered at the store seam and in the browser component spec. Also untouched: `AlignmentGrid.svelte` keeps its own local `isVariationColumn` for cell shading, and drag-and-drop of chips was not built, per the ticket's out-of-scope list. **Adversarial review then found nine defects; each fix carries red-before evidence against the pre-fix code, every fixture driven through `collateToAlignmentSnapshot()`/the real adapter.** **(1) Undoing a bulk verb destroyed unrelated edits, across units — fixed as the repo owner ruled.** `commitReadingsForUnit` snapshotted the whole `classifiedReadings` map, while `updateReadingText`, `updateReadingTextForDisplayMode`, `addReading`, `deleteReading`, `moveReadingByOffset`, `moveReadingBefore`, and `promoteReadingAsFamilyParent` wrote that map through `setReadingsForUnit` with no command, so their edits sat in neither the before nor the after copy and any later undo reverted them — in *any* unit, because the map is global. This ticket's note recorded that as a scope call; it was data loss. Every proposal mutator now commits: one gesture, one history entry (User Stories 6 and 9), and `setReadingsForUnit` has exactly one caller so nothing can write the map invisibly again. The snapshot is unit-scoped as well — completeness and blast radius were separate defects. **Two green specs encoded the missing wiring and were restated rather than deleted**: ticket 05's `keeps the lemma decision through an unrelated edit and undoes it in one step` asserted that one undo reverts the lemma and *leaves* the text edit standing, which is a scholar losing a text edit they never asked to keep; it now asserts what a scholar wants — the text edit undoes as its own step, the lemma decision beneath it is untouched until its own undo, and both redo. Ticket 02's cross-text attachment spec was restated the same way. Reverting `updateReadingText` alone reddens four specs, including a new one that moves witnesses in unit 1, edits text in unit 2, and asserts one undo touches only the second. **(2) Merge detached a hand-attached subreading.** `mergeReadings` reparented children in the proposal, but `setReadingsForUnit`'s anti-welding guard (ticket 02) restores `parentReadingId`/`isSubreading`/`autoGenerated` from the stored proposal for exactly the readings carrying a `subreadingOf` decision — so the reattachment was discarded for precisely the attachments a scholar made by hand, leaving `parentReadingId: null` and an orphaned decision. An attachment is a decision, so merge now retargets the decision to the survivor and commits it with the proposal edit as one gesture. The existing spec passed only because it built the subreading with `addReading({ parentReadingId })`, writing the proposal and never touching the decision path — green and inert in the way ticket 03 was; it now attaches across differing text with `setReadingParent`, asserts no orphan is reported, and asserts one undo restores the prior attachment. **(3) Both bulk verbs minted empty lettered readings.** The `whole-reading` guard fired only for a single source reading, so `splitWitnessesIntoNewReading` on `a:[A] b:[B,C] c:[D,E]` left `{a:[A], b:[], c:[], d:[B,C,D,E]}` and a move emptying `b` left `{a:[A,B,C], b:[]}` — an empty reading keeps its letter, its ordering position, its apparatus citation, and a stemma node. Of the two options the review offered, the verbs now **drop a reading they empty**, which is what merge already did and what a scholar means. A reading that was already empty stays (a conjecture a scholar added) and so does one that still carries an attesting subreading. **(4) Selection and the acted-on set could diverge.** `chips` is built from *visible* witnesses (twelve per reading), and the verbs took their witnesses from the selected chips, so selecting twenty and collapsing the reading moved twelve and announced twelve — silently, in exactly the many-witness case this ticket exists for. Selection is now read from the selection itself in reading order, never from the chips; the count, the sigla, and all three verbs agree. A browser spec expands a fourteen-witness reading, selects all of it, collapses it, and asserts all fourteen move. **(5) Focus was dropped to `<body>` after every verb**, because each ends in `clearSelection()`, which unmounts the selection bar holding the control being operated. Focus now lands on the chip of the first witness acted on; both verb specs assert `document.activeElement`. **(6) A `<select>` triggered two destructive verbs.** Move and merge acted on `change`, and a closed `<select>` in Chromium changes value on arrow keys — Tab-then-Down performed a move (ticket 08's review flagged the same pattern). Both now take a target and then an explicit `Move`/`Merge` press; a spec arrows through the closed select and asserts nothing moved and nothing was announced. **(7) The delete guard used the wrong witness set**: it tested the reading's own witnesses while the card displays subreading witnesses rolled up, so a main reading whose witnesses had been moved away unlocked Delete while sigla were still on the card. Store and component now read one set, `getAttestingWitnessIdsForReading`, which is the reading's own plus its whole subreading chain. **(8) Smaller.** A move announced every selected siglum even when fewer moved — `moveWitnessesToReading` now returns `movedWitnessIds` and only those are named. A merge discards the text of every reading it collapses (User Story 9), so the surviving-reading choice now shows a warning naming that text before the scholar commits, and the announcement repeats it. Merging away the lemma reading is allowed but no longer silent: the warning and the announcement both say the lemma will fall back to the base text's reading. `Escape` clears from the selection bar, not only from a focused chip. `page as any` is gone from the browser spec — the types were fine without it. The subreading-chain walk existed in four near-identical copies with two cycle semantics (null versus the last node visited); they are now one exported `makeMainReadingIdOf` used by `relabelReadings`, `applyDecisions`, `projectLocalStemma`, and the store's families, answering null on a cycle. **(9) Both deviation notes were rewritten in the ticket**: the missing Playwright spec is stated as a cost decision rather than an impossibility, naming what an isolated-component substitute cannot see (finding 5 is exactly that blind spot); the undo-granularity note is no longer a scope call, since finding 1 is fixed. Also noted rather than claimed: `splitWitnessesIntoNewReading`'s `subreadingOf` option is contract-specified and spec-covered but has no production caller. Server suite 687 tests (7 new, 4 restated), client browser project 262 tests (5 new), `pnpm check` clean, `pnpm lint` unchanged from baseline (the `no-useless-assignment` error in `reference-editions/insertion.ts` and 21 unused-var warnings, none in files this ticket touched). The one client failure is the known unrelated transcription-editor flake, which passes in isolation; its stray `__screenshots__/` directory was removed. |
| 2026-08-14 | Ticket `08` completed. The local stemma is now a single-source tree in the interface over arc storage, built entirely from a labelled control in a list beside the diagram — the accessible route lands before ticket 09's pointer and keyboard graph gestures, so the phase is fully operable without them. **A source decision is `undecided`, `unclear`, or `derived from` a named prior reading.** The three are stored where each can be told apart: `derived` is an arc, because arcs are the persisted form and the only shape that can hold more than one source; `unclear` rides the ticket-02 decisions overlay as `UnitDecisions.sourceDecision`, since no arc can express it; `undecided` is the absence of both and is never reported as a judgement. There is one place they are joined — `collation-stemma.ts`'s pure `projectLocalStemma(readings, arcs, lemmaReadingId, sourceDecisions)` — and `collationState.getLocalStemma()` is the store's only derivation of it, so no surface pairs readings with arcs itself. Subreadings fold into the main reading at the head of their chain and are never nodes; an arc recorded against a subreading attaches to that main reading; non-attestation never reaches the projection at all (ticket 03's data), and a reading flagged `isLacuna` by a pre-ticket-03 document is filtered out as well. A reading with two recorded priors is a `violation`: it is reported, its node projects **no** source, and nothing is drawn for it — a guess would be a rendering the data does not support. `setReadingSource` is the only mutation the stemma UI performs; it replaces every arc into a reading with at most one, refuses a cycle and a self-source observably, refuses a subreading at either end, and is one undo entry snapshotting both the arc map and the overlay. **The lemma is the default root and no code roots on witness count**: `suggestStemma`'s majority heuristic, `addStemmaEdge`, `removeStemmaEdge`, and the never-written `stemmaNodes`/`StemmaNode` are deleted rather than carried forward. All geometry is in the pure `collation-stemma-layout.ts`: layered top-down, roots at the top, node rects and cubic arc paths, viewport fitted to computed `bounds` — the hardcoded `600×200` viewBox and the old subreading-attachment positioning are gone, so the diagram makes one claim and four generations lay out without overlap. Undecided and unclear readings still get positions; they are visible and unconnected, and are told apart in both surfaces (dashed outline plus `?` versus a warning outline plus `unclear`, and a labelled state chip in the list). **`StemmaEdge` was renamed to `ReadingArc { id, priorReadingId, posteriorReadingId }`**, dropping `directed` per the ticket and the reserved-`source` rule in the SPEC (`sourceReadingId` had been breaking it); the state map is `readingArcs` and the persisted stemma node's field is `arcs`. Consequence, made loud rather than hidden: a document or workspace snapshot written before this change loses its arcs on load. **The earlier claim here that those arcs were unusable was wrong** — the old `StemmaPhase.handleNodeClick` wrote `{ sourceReadingId, targetReadingId, directed: true }` from real reading ids, field-for-field the new `ReadingArc` minus `directed`, so a scholar’s arcs were fully recoverable and the rename silently discarded them. `COLLATION_CURRENT_VERSION` is therefore bumped 3→4 with **no upgrader**, per the SPEC’s not-deployed position: `migrate-on-read` finds no upgrader for v3 and throws `invalidSchemaVersion`, so a pre-change document is refused with a clear message instead of parsing fine, hydrating to `readingArcs: []`, and having the loss written back on the next `markUnsaved()`. The fixture content hash was regenerated through `scripts/regenerate-collation-fixture-hash.ts` (unchanged — the fixture’s `stemma` is `null`), never hand-edited. Every new spec was proved by mutation before being trusted, with all fixtures driven through `collateToAlignmentSnapshot()`/the store rather than hand-built cells: removing the subreading fold reddens the fold and lacuna specs; projecting the first of two priors reddens the violation spec; ignoring the overlay reddens the unclear-versus-undecided spec; deriving `isLemma` from witness count reddens the lemma-root specs in both the projection and the store; pinning the layout to two rows and a 600×200 canvas reddens the four-generation and bounds specs; dropping the cycle fallback, reversing arc direction, and skipping unconnected nodes each redden their own spec; and in the store, keeping old incoming arcs, skipping the cycle check, omitting the arc map from the undo snapshot, never clearing an `unclear` entry, folding a subreading into the target instead of refusing it, and clearing a unit's arcs inside `setReadingsForUnit` each redden exactly one spec. Server suite (664 tests, 22 new), `pnpm check`, and the client browser project pass — the one browser failure, `transcriptionEditorWorkarounds`, is an unrelated flake that passes in isolation and left a stray `__screenshots__/` directory, removed. `pnpm lint` is unchanged from baseline (the `no-useless-assignment` error in `reference-editions/insertion.ts` and 21 unused-var warnings). **Not verified by hand in the running app**: the store and both pure modules are covered at their seams, but nothing exercises the rewritten `StemmaPhase.svelte` above `pnpm check` — the keyboard walk-through the ticket describes was not performed. Deliberately left for later tickets: the lemma-as-posterior contradiction, the reroot repair, and connectivity are ticket 10; drag and keyboard lift-and-place, live-region announcements, and focusable node controls are ticket 09, so this ticket's diagram is read-only and all editing goes through the list; `<graph>`/`<node>`/`<arc>` export is ticket 11, which should note that `unclear` serialises as the absence of an incoming arc while `undecided` must not serialise at all, and that the overlay deliberately holds only what arcs cannot. **Adversarial review then found ten defects; each fix carries red-before evidence against the pre-fix code, with every fixture driven through the real adapter/`collateToAlignmentSnapshot()` or the real seal/parse path.** **(1)** The persisted-shape break was silent and its recorded justification false — corrected above; a new `formats.spec` seals a v3 envelope carrying a scholar’s `edges` under a *correct* revision hash and asserts `invalid_schema_version`, and against `COLLATION_CURRENT_VERSION = 3` that document reads `ok: true`. **(2)** A `derived` source decision orphaned silently: `projectLocalStemma` dropped arcs whose endpoints no longer resolved, and `findOrphanedUnitDecisions` walked the overlay only — which holds `unclear` but never `derived`. So SPEC line 29 / User Story 5 held for `unclear` and failed for exactly the decisions the diagram is made of. The projection now returns `orphanedArcs`, `getOrphanedDecisionsForUnit` reports them as a `sourceArc` orphan, and `findOrphanedUnitDecisions` takes the arc map so a dead unit’s arcs are reported too. The spec that asserted the silence as correct (`ignores an arc that names a reading the unit no longer has`) now asserts the loud behaviour, and a store-level spec deletes a reading another reading derives from and asserts the report. **(3)** `splitWitnessFromReading` had lost its only UI route when the old stemma reading list was deleted; the readings phase’s per-witness reassign menu now offers `Split into a new reading` alongside the destination readings, reusing the existing store function — the readings table is untouched, since rebuilding it is ticket 07. **(4)** The refusal barely reported itself: it now renders beside the select that produced it rather than at the top of a scrolling `w-96` panel, is keyed by a counter so a repeated identical `cycle` refusal re-announces, and is scoped to the unit and reading it names so it cannot outlive either. **(5) Recorded as a deviation, not built**: the diagram gained a `<title>`/`<desc>` naming every node’s label, text, lemma status, and source state, but it is still one `role="img"` graphic — per-node programmatic state needs focusable node controls, which is ticket 09, so that acceptance criterion is left **unchecked** with the deviation written into the ticket. **(6)** Node colour conflated two axes: `stateOf` returned `lemma` whenever the node was the lemma and had no source, so an unconsidered lemma rendered as settled and all lemma marking vanished once it derived. Colour now encodes the source decision alone (SPEC line 201) and the lemma takes a separate inset primary outline and `lemma` marker; the legend documents all four source states plus the lemma axis. **(7)** A conflicting-sources node reported `Undecided` in its own select; it now shows a disabled `Conflicting sources` placeholder, so undecided is never presented where two judgements are recorded. **(8)** The svg was `w-full h-full` with `preserveAspectRatio` inside an `overflow-auto` box, so overflow never triggered and eight undecided readings (1256px wide) were crushed to roughly 6px of reading text in a ~700px panel — User Story 46, the reason the ticket exists. It now renders at its computed `bounds` and the container scrolls. **(9)** `setReadingSource` wrote unconditionally, so choosing `Undecided` on an untouched unit created empty `unitDecisions`/`readingArcs` entries — a worklist item for a judgement never made, in the report ticket 06 built to be trustworthy — plus a dead undo entry and a spurious unsaved mark. It now compares the intended arcs and overlay against the recorded ones and returns `{ ok: true }` without writing, following `setLemmaReading`’s existing guard. **(10)** Small fixes: the `cycle` refusal text described self-source; `getSpanLabel` returned `0` for an index it could not find, so the heading could read “Unit 0”; the unit tab strip conveyed selection by colour only and now carries `aria-current` plus a weight/underline cue; and the arrow-key handler now exempts `TEXTAREA` and `contenteditable` and only `preventDefault()`s the keys it handles. Left alone as noted inert: `setReadingSource`’s `ensureReadingsForUnit` side effect. Server suite 671 tests (7 new), client browser project 249 tests, and `pnpm check` pass with no stray `__screenshots__/`; `pnpm lint` is unchanged from baseline (the `no-useless-assignment` error in `reference-editions/insertion.ts` and 21 unused-var warnings). |
| 2026-08-14 | Ticket `06` completed, then remediated after adversarial review. `collation-apparatus.ts` is the one derivation of a verse's shape — pure, no store, no runes, no DOM — exporting `buildSegmentSequence` (alternating agreed stretches and variation units, every column covered exactly once) and `renderApparatusUnit` (`2-6 ⸂ λογος ] a: A B \| b: C \| zz: D`), plus `buildDisplayedColumnSlots`, which the store's `getDisplayedColumnSlots` now delegates to so the word-index labels have a single source. `ReadingsPhase.buildBasetextSegments` is deleted: the strip, the unit list, the unit ordinals, and the new live preview all read `collationState.getSegmentSequence()`. The preview is a labelled `<section>` of real selectable text, updates through `$derived` as decisions change, cites subreadings under their main reading, shows reading-type labels from the project vocabulary, and pins non-attestation last under the reserved label `zz`; untranscribed witnesses are deliberately not cited there, because the notation would assert damage to a manuscript that may be intact. **Review found the first cut had solved the central rule in the wrong place, and the repo owner approved the scope expansion the ticket had excluded — changing how spans are computed.** The root cause: `buildVariationUnitSpans` counted a non-attesting witness's gap cell as disagreement, so every column a damaged or untranscribed witness covered became a spurious variation unit. The first cut worked around that inside the renderer (`spanShowsVariation`), which left two production definitions of "variation unit" — and the units they disagreed about were reachable: a unit `getUnitsNeedingLemmaDecision()` reported could not be opened in the readings pane, because `selectedSpan` fell back to `spans[0]` and wrote the fallback back, so ticket 05's worklist linked to a unit that snapped away; and a decision recorded at such a unit was persisted, invisible, and unreportable, since `getOrphanedUnitDecisions()` still counted it live. The fix is at the source: `isVariationColumn` now judges agreement over *attesting* witnesses only (ticket 03's `classifyWitnessAttestation` is the authority), and `spanShowsVariation` is deleted, so `getSegmentSequence`, `getVariationUnitSpans` (the stemma phase, `forEachUnitView`, `moveFocus`, the orphan report's live-unit set) and `collation-projection.ts` read one definition again. Fallout handled deliberately: a decision saved against a column that is no longer a unit is reported by `getOrphanedUnitDecisions()` rather than dropped — asserted by spec, and no upgrader is written, per the epic's not-deployed position. Two further defects fixed: `citationOrder` paired mains only with *direct* children, so a subreading nested two levels deep — reachable from the readings phase, whose "Attach as subreading of" select offers every main on every row — appeared in neither `mains` nor any filter and its witnesses vanished from the apparatus entirely; citation now walks the whole chain and cites every reading exactly once, and `relabelReadings` labels a chained subreading flat from the main at the head of its chain (`a1`, `a2`) instead of `?`. And `renderApparatusUnit`'s `nonAttestation` option is now **required**, not optional, so no caller (ticket 11 included) can silently produce an apparatus with no reserved `zz` entry; a `@ts-expect-error` spec makes `pnpm check` fail if it is ever made optional again. The shared-ness spec was rewritten: the old one compared `getSegmentSequence()` against a helper that called the same two functions, and could not fail. It now states the units the witnesses themselves imply and asserts each consuming surface against that — the segment sequence, `getVariationUnitSpans`, the projection, `getLemmaDivergence` (through `forEachUnitView`), the orphan report, and keyboard unit navigation — and was proved by mutation (making the sequence drop a span reddens it). Every fix carries red-before evidence against the pre-fix code, with all fixtures driven through `collateWitnesses()`/the real adapter: reverting the attestation rule reddens four specs (spurious unit, shared-ness, worklist selectability, orphan reporting); reverting `citationOrder` drops witness `C` from the notation outright; making `nonAttestation` optional fails `pnpm check`. **One acceptance criterion is not met, and is recorded as an approved deviation rather than as satisfied**: the basetext strip does *not* render identically to before for a collation with no non-attestation — an agreed run is now one segment carrying one word-index range label (`2-8 εν χριστω ιησου τω`) instead of one label per word. That is what makes the sequence alternate and what ticket 11 needs to emit contiguous agreed text; unit chips are unchanged. Server suite (640 tests), client browser project (249 tests), and `pnpm check` pass; `pnpm lint` is unchanged from baseline (the `no-useless-assignment` error in `reference-editions/insertion.ts` and 21 unused-var warnings). **For ticket 11**, which will cite these ranges: `buildDisplayedColumnSlots` treats the base witness's `⊘` gap cell as a base-text word, so a column no witness attests still consumes a word-index slot while contributing no text — pre-existing, left alone here, and to be ruled on when the export fixes the citation ranges. Also left for ticket 11: TEI serialisation from this same `Segment[]` and unit view. Left for ticket 07, which rebuilds the readings table: `getReadingFamiliesForUnit` still assumes one level of subreading, so a chained subreading is cited correctly in the apparatus but not shown as a row; and `AlignmentGrid.svelte` keeps its own local `isVariationColumn` for cell shading in the alignment phase. |
| 2026-08-14 | Ticket `04` completed. A reading now carries an evidential **reading type** from an open, project-extensible vocabulary (`reading-types.ts`: omission, deficient, apparent, nonsense, orthographic, plus whatever a project adds through `ProjectCollationSettings.readingTypes`) and an orthogonal `certainty`. Both are proposed then decided: `proposeReadingType` derives a type from evidence the pipeline already carries — omission from the aligner, `deficient` from segments the transcriber marked unclear or supplied, `nonsense` from a `ns`-typed regularization rule having fired — and `UnitDecisions.readingType` / `.certainty` ride the ticket-02 overlay, so a recorded value outranks the proposal, a recorded `null` means "no type" rather than undecided, each write is one undoable command, and a decision naming a vanished reading orphans instead of disappearing. The incoherent control is gone: the readings-phase Type select now reports exactly what it writes, offers only genuine editorial judgements (omission is shown when the aligner determined it but cannot be picked, and `lac` — ticket 03's leftover hand-created lacuna — is retired, so nothing in the app sets `isLacuna` on a reading any more), and **no type change touches `text` or `normalizedText`**, which removes the silent text destruction rather than gating it behind a confirmation. `ReadingClassification`, `classifyReading`, and the stemma node tinting derived from it are deleted; nodes take one neutral colour until ticket 08 gives colour a meaning. Subreadings carrying no type are recorded through `getSubreadingsMissingReadingType()` for ticket 11 to surface. Reading types no longer follow family position: promoting a family member keeps each reading's own type, which corrected three specs that had asserted the old blanket `ns`. New signals (`hasUnclear`, `hasSupplied`) are computed on the reading buckets from `originalSegments`, and the proposal specs are driven through the real adapter — both were confirmed red against a mutated proposer before being relied on. Adversarial review then caught four defects, each fixed behind a spec confirmed red against the pre-fix code and driven through the real collation adapter rather than hand-built cells. **(1)** Every mutator hands the decision-applied view back to `setReadingsForUnit`, which stores it as the proposal, so a recorded type or certainty was welded into `classifiedReadings` — it survived its own undo, stopped re-deriving when its evidence changed, and was what `buildApparatus` persisted. `setReadingsForUnit` already restored `parentReadingId`/`isSubreading`/`autoGenerated` for the same reason in ticket 02; it now restores every field a decision can overlay, and the round-trip spec runs through the undo rather than stopping at the edit. **(2)** The reading bucket key folded segment text and `hasUnclear` but not `isSupplied`, so under the default `suppliedTextMode: 'clear'` a witness reading `λ[ογος]` bucketed with witnesses reading `λογος` intact, `hasSupplied` was OR-ed across the bucket, and `proposeReadingType` returned `deficient` for all of them — asserting of an undamaged base text that its reading is only partly identifiable. `isSupplied` is now part of the key, as `hasUnclear` always was. **(3)** `coerceReadingTypeDefinitions` let a project blob `{ id: 'omission' }` default `selectable` to true and so put omission back in the menu; the parser now takes a bundled type's selectability as authoritative and lets a project override only its label and description. **(4)** `reset()` did not clear `projectReadingTypes`, so `createProject` copied one project's vocabulary into the next one created. The stemma phase's stale "Reading Classification" heading and comments are gone. A note went to ticket 11 recording that the legacy TEI exporter emits `readingType` as `@type` without distinguishing a proposal from a decision, which ticket 11 must rule on when it replaces that exporter. Left alone as out of scope: the `persistRulesToProject` race and the identical pre-existing sloppiness for witness treatments. Server suite (629 tests), client browser project (249 tests), and `pnpm check` pass; `pnpm lint` is unchanged from baseline (the `no-useless-assignment` error in `reference-editions/insertion.ts` and 21 unused-var warnings). Not done here and deliberately: reading-text edits remain outside the undo history (ticket 07 rebuilds that table), and `@type`/`@cert` serialisation stays with ticket 11. |
| 2026-08-13 | Ticket `03` completed after a remediation pass. The first attempt shipped green but inert: `classifyWitnessAttestation` decided attestation from text presence (`!cells.some(cell => cell.text?.trim())`, inherited verbatim from the old `isLacuna` bucket predicate), while every real gap cell carries text — the tokenizer emits `⊘` for gap and untranscribed milestones and a supplied-only token collated as a gap keeps the editor's restored letters. A genuine `<gap>` witness therefore still took a letter, still counted in ordering, and would still have become a stemma node; the lacuna buckets it derived from had never formed either. The specs passed only because their fixtures built cells with `text: ''`, a shape `toCellText` never returns. Attestation is now decided from the fields that carry the information — `cell.kind` and the damage flag — so gap, untranscribed, and supplied-as-gap cells are absent testimony whatever text they carry, and a slot emptied by `shiftToken` or `mergeCells` while still flagged damaged is absence rather than an omission the witness attests. `collation-projection.ts` no longer borrows another reading's text for `base_text` when the base witness does not testify. Non-attestation is otherwise as specified: `buildReadingProposal` returns `{ readings, nonAttestation: { witnessIds, untranscribedWitnessIds } }`, non-attesting witnesses never reach `buildReadingFamilyGroups`, lettering and ordering are identical whether a witness is lacunose or simply absent, `setReadingParent` refuses non-attestation in both directions because it holds no reading id, `alignment-snapshot.ts` recovers `untranscribed` from `gap.source` for cells stored without a kind, and the readings phase pins one non-attestation row last with damaged and not-yet-transcribed sigla listed separately. The lacuna fixtures are now built by running the real adapter over a gap token, and a store-level spec drives a real `<gap>` witness through collation and asserts it takes no letter and lands in `nonAttestation.witnessIds`; every corrected spec was confirmed red against the pre-remediation classifier. Server suite (604 tests), client browser project (249 tests), and `pnpm check` pass; `pnpm lint` is unchanged from baseline (the `no-useless-assignment` error in `reference-editions/insertion.ts` and 21 unused-var warnings). |
| 2026-08-13 | Ticket `05` completed. The lemma is now an editorial decision: `UnitDecisions.lemmaReadingId` rides the ticket-02 overlay, `relabelReadings` takes the lemma so it labels `a` while the base-text reading keeps its place ahead of the rest, and a base text that is lacunose or excluded yields no lemma — the unit is reported as needing a decision instead of promoting the majority reading. Divergence and the needs-decision worklist are queryable from the store; elevation is one undoable command and never touches arcs; refused reorders now return a reason. Review caught that the derived readings' `order` field did not follow the lemma-first labelling, so `collation-projection.ts` and the TEI `<lem>` selection would have disagreed with the letters a scholar saw; `relabelReadings` now stamps the rank its own labelling implies, which also fixed reorder acting on a different row than the one displayed. `buildApparatus` was not passing the base witness, so a derived lemma was lost on save. Server and client vitest projects, `pnpm lint` (bar the known `no-useless-assignment` baseline error), and `pnpm check` pass. |
| 2026-08-12 | Tickets `01` and `02` completed. The reading proposal is now a pure module; sparse subreading editorial decisions are keyed by stable variation-unit identity, applied on read, persisted independently of derived readings, preserved as orphaned decisions, and covered by phase-aware undo. Focused decision/state/persistence tests and `pnpm check` pass. Full acceptance commands remain affected by unrelated baseline failures: `pnpm lint` reports `no-useless-assignment` in `reference-editions/insertion.ts`, and full browser collection intermittently fails unrelated editor/Data & Storage specs. |
| 2026-08-11 | Ticket `02` rewritten after an architecture review, and `SPEC.md` corrected. Two findings. First, an **editorial decision** had no durable address as well as no durable identity: decisions and stemma edges are keyed by `getReadingUnitKey`, which returns a position in `alignmentColumns`, while `mergeColumns`, `splitColumn`, and `shiftToken` all reindex that array and touch neither map. Splitting proposal from decision without fixing the key would have produced decisions that persist faithfully under the wrong **variation unit** — worse than ones that vanish. The identity is already computed and discarded: `collation-document.ts` builds `unit:${columnId}` on write and re-keys by `unitIndex` on read. Ticket 02 now carries the key change; the blast radius is one function and its six call sites, and `stemmaEdges` follows for free because the stemma functions call the same helper. Second, `SPEC.md` claimed re-running an alignment stops destroying editorial work. It cannot: `collation-adapter.ts` assigns every column a fresh `crypto.randomUUID()` per run, so nothing column-derived survives a re-collation and reattaching needs a content-derived anchor nobody has designed. The SPEC now scopes the claim to hand edits and states the re-collation gap explicitly, and ticket 02 forbids inventing an anchor. `CONTEXT.md` gained a **Variation unit** entry. |
